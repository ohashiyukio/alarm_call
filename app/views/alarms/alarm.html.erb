<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>アラーム</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
      text-align: center;
    }
    #alarmMessage {
      font-size: 1.5em;
      margin-top: 20px;
    }
    button {
      margin-top: 20px;
      padding: 10px 20px;
      font-size: 1em;
    }
  </style>
</head>
<body>
  <h1>アラーム</h1>
  <div id="alarmMessage">2024-11-21 08:00 - 朝のジョギング</div>
<button id="enableAudio">アラームを有効にする</button>



  <audio id="alarmSound" loop>
    <source src="alarm.mp3" type="audio/mpeg">
    Your browser does not support the audio element.
  </audio>

<script>
  const alarms = [
    <% @alarms.each do |alarm| %>
      {
        time: "<%= alarm.alarm_time.strftime('%Y-%m-%d %H:%M:%S') %>",
        audioPath: "/alarm_<%= alarm.id %>.mp3",
        comment: "<%= alarm.comment %>"
      },
    <% end %>
  ];
</script>
</body>
</html>
